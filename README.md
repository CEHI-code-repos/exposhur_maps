## To make maps:

Set up the development environment:

```{bash}
nix develop
```

And download data from [CEHI-code-repos/exposhur-measures/output/](https://github.com/CEHI-code-repos/exposhur_measures/tree/main/output) into a `data` folder.

Then run `exposhur_maps.R`
